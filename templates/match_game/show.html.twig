{% extends 'base.html.twig' %}

{% block title %}MatchGame{% endblock %}

{% block body %}
    <h1>MatchGame</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ match_game.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_match_game_index') }}">back to list</a>

    <a href="{{ path('app_match_game_edit', {'id': match_game.id}) }}">edit</a>

    {{ include('match_game/_delete_form.html.twig') }}
{% endblock %}
